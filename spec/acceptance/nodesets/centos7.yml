HOSTS:
  centos-7-x64:
    platform: el-7-x86_64
    hypervisor : docker
    image: centos:7
    docker_preserve_image: false
    docker_cmd: '["/usr/sbin/sshd -D"]'
CONFIG:
  type: foss
  log_level: debug
ssh:
  password: root
  auth_methods: ["password"]
